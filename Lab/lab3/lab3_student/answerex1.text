    .data：该伪指令表示数据段的开始，通常用于存储变量和常量。在你的程序中，n: .word 9 存储在 .data 段中。

    .word：该伪指令分配一个字（4字节）的内存并初始化为指定的值。在这里，n 被初始化为 9。

    .text：该伪指令表示代码段的开始，程序的实际指令存储在这里。

伪指令：
    la（加载地址）：该伪指令会被汇编器转换为 lui（加载高位立即数）和 addi（加立即数）指令的组合。它用于加载标签的地址（例如，la t3, n 将 n 的地址加载到寄存器 t3 中）。

    mv（移动）：该伪指令会被转换为 addi 指令，源寄存器为 x0（例如，mv t0, t1 被转换为 addi t0, t1, 0）。

    j（跳转）：该伪指令会被转换为 jal（跳转并链接）指令，链接寄存器为 x0（例如，j fib 被转换为 jal x0, fib）

output: 34, meaning : the 9-th Fibonacci number.

address: 0x10000000
auipc